<!DOCTYPE html>
<html lang="en">
    
    <!-- THE HEAD TAG PARTIAL -->
    <%- include("../partials/head") %>

    <body>
        <!-- THE HEADER TAG PARTIAL -->
        <%- include("../partials/header") %>
        <main>
            <h2 class="subtitle is-2">Artists</h2>

            <a href="/dashboard">DASHBOARD</a>

               <h3 class="title is-3"><%= artist.text %></h3>

            
            <form action="/artists/<%= artist._id %>?_method=PUT" method="post">    
                <input class="input" type="text" name="text" placeholder="new artist" value="<%= artist
                
                
                Tz.text %>" />
                <input class="button" type="submit" value="edit artist" />
            </form>

            <form action="/artists/<%= artist._id %>?_method=delete" method="post">    
                <input class="button" type="submit" value="delete artist" />
            </form>


        </main>
        <!-- THE HEAD TAG PARTIAL -->
        <%- include("../partials/footer") %>
    </body>
</html>